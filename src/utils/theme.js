import {createMuiTheme } from '@material-ui/core'

const theme = createMuiTheme({
    palette: {
        primary: {
            main: '#3f51b5'
        },

        secondary: {
            main: '#003049',
        },

        type: 'dark'
    },
})

export default theme;